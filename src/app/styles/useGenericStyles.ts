import { createStyles } from "@mantine/core";

export const useGenericStyles = createStyles(() => ({
  // eslint-disable-next-line no-use-before-define
  hoverable: {
    hover: {
      cursor: "pointer",
    },
  },
}));
